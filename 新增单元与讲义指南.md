# 如何新增单元和讲义

词库的**唯一编辑入口是 `content/` 目录**，改完跑一条命令即可生效，不用碰代码。

## 目录结构

```
content/
├── meta.json                词库元信息（一般不用动）
├── doors.json               全部关卡门（20 扇门，顺序=地图顺序）
├── _template-unit.json      新单元模板（复制它起步）
└── units/
    ├── 01_u01-school-life.json
    ├── 02_u5u6-doctor-challenge.json
    └── ...                  每个单元一个文件，文件名开头的序号决定顺序
```

## 新增一个单元（例如收到新讲义）

1. **拍照存档**（可选但建议）：讲义照片放进 `assets/handouts/`，名字记到单元文件的 `sourceImages` 里，方便日后核对。
2. **复制模板**：复制 `content/_template-unit.json` 到 `content/units/`，改名如 `07_u11u12-weather-travel.json`（序号接着现有最大编号 +1）。
3. **填内容**：
   - `unitId` / `title`：单元 ID 和标题
   - `entries`：逐个词条填写。必填：`id`（建议 `u1112-01` 这样单元缩写+序号）、`type`（`word` 单词 / `phrase` 短语）、`text`、`meaning`、`door`（所属门编号）；`phonetic` / `examples` / `phrases` / `extensions` 有就填，没有留空
   - `dictationExtra`：校内默写补充词（不属于正文的易错词），没有就留 `[]`
4. **如需新门**：在 `content/doors.json` 末尾追加一扇门（`id` 顺延，如 21），并给门取名字和 `theme`。
5. **构建并自检**：

   ```bash
   npm run build:data        # 等价于 python3 tools/build-content.py
   ```

   脚本会先**校验**再生成：缺字段、id 重复、door 不存在会直接报错并指出是哪一条；词条重复、空门只警告。全部通过后自动更新 `data/words.json` 和 `data/words-data.js`。
6. **刷新页面**就能看到新内容（如果孩子正在玩，进度不受影响——词库和进度是分开存的）。

## 修改已有词条

直接编辑 `content/units/` 里对应的单元文件，再跑 `npm run build:data`。

## 岛屿顺序规则

**地图上的岛屿顺序 = `content/units/` 文件名开头的序号顺序。** 学习进度按岛屿顺序逐岛解锁，所以：

- 新增单元一律放在**最后**（取现有最大序号 +1，如 `07_xxx.json`），它会自动成为最后解锁的岛
- 想调整岛屿顺序，只需重命名文件改序号，然后跑 `npm run build:data`
- 当前顺序：健康岛 → 自然岛 → 社团岛 → 科学岛 → 未来岛 → 校园岛（校园是后续内容，故意放在最后）
- ⚠️ 改序号的同时要同步调整 `game.js` 里的 `ISLAND_META` 和 `INTROS` 数组顺序（它们和文件序号一一对应），改错会导致岛屿名字/开场白张冠李戴

## 注意事项

- **不要手改** `data/words.json` 和 `data/words-data.js`，它们是自动生成的，每次构建会被覆盖。
- 词条的 `door` 决定它出现在哪扇门；艾宾浩斯复习、默写、卡片全部自动跟随，无需额外配置。
- 旧脚本 `tools/build-data.py` 已被 `tools/build-content.py` 取代（旧的还留着，但别用了）。
